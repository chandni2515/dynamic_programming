package Practise;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Map;

public class FindShape {
/*
Assume there is a factory that produces 3 types of shapes: triangle, circle, square.

Each shape has a unique ID with a given type.

These shapes after productions are sent to a Warehouse.

The Warehouse should allows a client to lookup an item given a shape type and shape ID.

warehouse(shape)
warehouse(list<shapes>)-->retrieve unique type of shape with given id

same id can be assigned to different shape, return unique combination of shape with id.

eg: traingle with id-123



*/

/*    public class Main {
        public static void main(String[] args) {
            System.out.println("Welcome to Online IDE!! Happy Coding :)");

            List<Shape> sList = Arrays.asList({})
            Map<Integer, List<Shape>>
            findshape(123, sList);
        }

        public List<Shape> findShapes(int id, List<Shape> list) {
            List<Shape> result = new ArrayList<>();
            for (Shape s : list) {
                if (s.getid() == id) {
                    result.add(s);
                }
            }
        }

    }

    // First class to identify the shape
    public class Shape {
        int id;
        String shape;
        //getter setter constructor

    }

    class Warehouse {
        int wid;
        List<Shape> shapes;
    }*/
}